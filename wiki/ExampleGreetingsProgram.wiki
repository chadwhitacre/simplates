#Greetings program simplates example

= Introduction =




= Details =
@ urls.py

{{{

from django.conf.urls.defaults import *
from project.simplates.views import direct_to_simplate
urlpatterns = patterns('',
    (r'^greetings/(?P<program>[^/]+)/$', direct_to_simplate, {'simplate':'index.html'}),
)

}}}


@ Simplate:

{{{
# Import section
# ==============

 # ASCII form feed (this character is also called a page break, FF, <ctrl>-L, 0xc, 12).

# Python runtime section
# ======================

try:
    program = params['program']
except KeyError:
    program = 'World'

 # ASCII form feed

<!-- Django Template -->
<p>Greetings {{program}}!!</p>
}}}